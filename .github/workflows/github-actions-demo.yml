name: List Posts in the Repo
on: [push]
jobs:
  List-Posts:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}/posts/*.html > posts.txt
